export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <span className="font-mono text-sm opacity-50">Loading initialization sequence...</span>
    </div>
  );
}
